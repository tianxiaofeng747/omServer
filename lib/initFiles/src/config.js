seajs.config({
    // 调试模式
    debug: true,

    // Sea.js 的基础路径
    base: '../',
    //base: location.hostname.indexOf("52shangou") != -1 ? '../' : '//localhost:8000',

    // 文件编码
    charset: 'utf-8'
});
