seajs.config({
    "paths": {
        "src": "../src"
    },
    "alias": {
    <% tagList.forEach(function(tag, index) { %>
        <% if (tag.moduleName === 'lib.mtop') { %>
        "lib.mtop.middleware": "//g.alicdn.com/mtb/<%= tag.filePath %>/<%= tag.version %>/middleware.js",
        <% } %>
        "<%= tag.moduleName %>": "//g.alicdn.com/mtb/<%= tag.filePath %>/<%= tag.version %>/<%= tag.fileName %>.js"<% if (tagList.length !== index+1) { %>,<% } %>
    <% }) %>
    }
});